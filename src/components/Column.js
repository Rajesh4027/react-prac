import React from 'react'

function Column() {
  return (
    <React.Fragment>
        <td >Name</td>
        <td>Disha</td>
    </React.Fragment>

    // we can add react fragments in this way but we do not pass the key attribute
    // <> 
    // </>
  )
}

export default Column